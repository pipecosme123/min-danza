# Se "hornea" en el build (npm run build) -- no es una variable de runtime.
# Ajustar al subdominio real del backend antes de desplegar
# (ver docs/deployment/cpanel-deployment-plan.md §1.5/§2).
VITE_API_URL=https://server.mindanza.lluviasdebendiciones.com/api
