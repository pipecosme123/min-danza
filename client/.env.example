# URL base de la API. En desarrollo puedes dejarla vacía (o apuntando a
# "/api") y usar el proxy de vite.config.js hacia el backend local; en
# producción debe ser la URL pública completa de la API, ej.
# "https://api.tudominio.com/api".
VITE_API_URL=/api
